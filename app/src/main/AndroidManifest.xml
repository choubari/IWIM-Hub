<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.choubapp.iwim_hub">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AjouterMatiere"></activity>
        <activity android:name=".Abscences" />
        <activity android:name=".Messagerie" />
        <activity android:name=".Matieres" />
        <activity android:name=".Timetables" />
        <activity android:name=".RedirectionToLists" />
        <activity android:name=".UserData" />
        <activity android:name=".ListEtudiants" />
        <activity android:name=".ListProfesseurs" />
        <activity android:name=".Dashboard" />
        <activity android:name=".Registration" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>